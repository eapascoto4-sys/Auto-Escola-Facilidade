{
  "name": "auto-escola-facilidade",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css script.js logo-auto-escola-facilidade.jpeg dist/"
  }
}
